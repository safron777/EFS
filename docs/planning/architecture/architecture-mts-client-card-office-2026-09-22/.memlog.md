---
scope: Backend for mts-client-card-office: DDD microservices on Java + PostgreSQL, derived from the existing React frontend (src/api/client.js, src/api/mockData.js as draft data contract)
purpose: Fix invariants for backend build (service boundaries, DDD paradigm, data ownership) so independently built microservices stay consistent
altitude: initiative (no PRD/spec input yet; frontend prototype is the only settled artifact)
updated: 2026-09-23T22:12
---

- (decision by user) Working mode: Coaching path (open-ended elicitation, user makes the load-bearing calls)
- (note by user) Deliverable: spine (ARCHITECTURE-SPINE.md) is not the only output -- a human-readable rendering is also needed; audience/format to be pinned down next
- (decision by user) Audience: both — technical team session/discussion AND non-technical leadership sign-off at МТС Банк
- (decision by user) Renderings at Finalize: all three — interactive HTML+SVG deck, full HTML/MD solution design, C4 diagram set (Context/Container/Component) — in addition to ARCHITECTURE-SPINE.md
- (version) Java 25 LTS (released 2025-09-16) verified current LTS as of 2026-09; Java 26 exists but is non-LTS -- source: Oracle Java SE support roadmap / Wikipedia Java version history
- (version) Spring Boot 4.1.1 (released 2026-08-20) verified latest stable, requires Java 17+, compatible through Java 26 -- source: endoflife.date/spring-boot, codejava.net
- (version) PostgreSQL 18.6 (released 2026-08-13) verified latest stable major line; PostgreSQL 19 in beta -- source: postgresql.org release notes
- (decision by user) Scope tier: demo/prototype backend for the frontend prototype, not production-grade. External systems (SFM/FSSP/antifraud/monitoring) are stub adapters, not real protocol integrations -- swap-in point kept clean for later real integration
- (decision by user) Service granularity: fine-grained, one microservice per frontend product tab/domain (8 services): client-identity, card-account, card-issuance, operations, risk-blocks, cashback, notifications, incident-case
- (decision by user) Mass incidents ingestion: push/event -- monitoring system publishes events (e.g. MassIncidentOpened/Updated/Closed), incident-case-service consumes; resolves the open question from README section 'Вопросы, которые повлияют на архитектуру' item 3
- (decision by user) Message broker: Kafka (event bus for mass-incident push integration and future cross-service events)
- (decision by user) Data ownership: DB-per-service -- each of the 8 microservices owns its own PostgreSQL database/schema, no cross-service table sharing
- (decision by user) Build tool: Maven, one pom.xml per service
- (decision by user) Paradigm: DDD with one microservice per bounded context, hexagonal/ports-and-adapters internal to each service
- (decision by user) Repo layout: single new monorepo (separate from frontend repo EFS) -- 8 Maven modules, one docker-compose for all services + Kafka + per-service Postgres containers
- (decision by user) Frontend entry point: API Gateway (Spring Cloud Gateway) in front of the 8 services -- single base URL, matches existing single-fetchJson contract in src/api/client.js
- (decision by user) Money contract: backend returns amounts as integers in minor units (kopecks), never pre-formatted strings with sign/currency -- currency/locale formatting is a frontend concern
- (decision by user) Cross-context references are by ID only (e.g. clientId, cardId) -- no service stores a copy of another service's owned data; consumers fetch details through the Gateway on demand
- (decision by user) Link-field safety rule inherited from frontend IdentityBar.isSafeInternalLink (GHSA-wrjc-x8rr-h8h6): any backend response field carrying a link/URL must be a relative internal path only, never absolute/external
- (decision by user) Auth for demo: Gateway passes an employee-id header, services trust it as a stub -- sufficient for frontend to exercise its pending 401/403 handling (TODO in client.js). Real auth mechanism deferred, user flagged it for later but the intended term needs clarification
- (question) README open question 1 (employee role determination KC/Chat/Office) stays Deferred -- frontend only implements Office role today
- (decision by user) Real (deferred) auth mechanism: OAuth2/OIDC via corporate SSO -- clarifies the earlier garbled 'cloacl'; header-based employee-id stub remains for the demo, this is the future replacement, not built now
- (note by user) Tablet layout is a frontend/design-kit concern (responsive Granat 2.0 layouts), not part of this backend spine -- API contracts are device-agnostic; user confirmed, routed to the parallel design-kit track
- (decision) API style: REST over HTTP, resource paths mirror existing src/api/client.js shape (/clients/{id}, /cards/{cardId}/operations, etc.) [ADOPTED from existing frontend contract]
- (decision) Testing: each service ships integration tests against Testcontainers (Postgres + Kafka where applicable); no shared test DB
- (direction) Deployment/production infra (cloud provider, k8s, CI/CD) is Deferred -- demo scope only needs local docker-compose; revisit once this becomes a real production track
- (version) Apache Kafka 4.3.1 (patch on 4.3, latest stable as of 2026-09) verified -- source: kafka.apache.org release announcements
- (version) Maven 3.9.16 (released 2026-05-17) verified latest stable -- source: maven.apache.org download page / endoflife.date
- (version) Testcontainers for Java 2.0.5 (released 2026-04-20) verified latest -- source: github.com/testcontainers/testcontainers-java releases, mvnrepository.com
- (decision by user) Error envelope confirmed as proposed: { error: { code, message, details? } } + HTTP status for error class, uniform across all services
- (decision by user) Dates/timestamps on the wire confirmed: ISO-8601 UTC; locale formatting stays a frontend concern (same rationale as AD-3 money)
- (decision by user) getDocuments confirmed owned by card-account (as proposed)
- (decision by user) AD-1 amended: NINTH bounded context added -- offers-service. getOffer (top bank offer) does not belong to client-identity; it is its own service, backed by a stub port to the bank's external offer/marketing engine (same AD-10 pattern as risk-blocks' SFM/FSSP/antifraud ports), referenced from client-identity by clientId only (AD-2), called directly by the frontend via Gateway at its own endpoint (mirrors existing separate getOffer() call in client.js, no AD-9 violation)
- (event) spine finalized -- ARCHITECTURE-SPINE.md status: final, 11 ADs (9 bounded contexts), lint_spine.py clean (only false-positive {id} path-param matches), reviewer adversarial pass closed 3 divergence scenarios
- (note) Clarified ОМТ abbreviation in AD-1's notifications line, grounded in kbStubs.omt from mockData.js ('Основной метод связи с клиентом') -- gap flagged by the renderings subagent, closed without inventing new content
- (event) Renderings complete: interactive HTML deck (Artifact), solution-design.html, c4-diagrams.html under renderings/. Run closed.
- (decision by user) SUPERSEDES prior repo-layout decision: backend now lives at backend/ INSIDE the existing EFS git repository (Maven multi-module tree alongside src/), not a separate new monorepo. Decided during Story 0 setup in bmad-build. ARCHITECTURE-SPINE.md Structural Seed updated accordingly (root dir renamed backend/, note added).
- (decision by user) AD-12 (new, added during Story 1 planning in bmad-build): schema migrations via Liquibase, one changelog set per service under src/main/resources/db/changelog/, applied automatically on service startup. Chosen over Flyway/Hibernate ddl-auto when the first entity-bearing story (Story 1, client-identity) needed a real answer -- ddl-auto:none had left every service with zero tables since Story 0. Applies uniformly to all 9 services.
- (version) Liquibase 5.0.3 (released 2026-05-15) verified latest stable -- source: github.com/liquibase/liquibase releases, endoflife.date/liquibase. Version actually used will be whatever the Spring Boot 4.1.1 BOM pins for liquibase-core (not overridden), so services stay on Boot's tested pairing rather than the bleeding edge.
- (decision by user) Error-code vocabulary decided (resolves SPEC.md open question, added during Story 1 planning): NOT_FOUND, VALIDATION_ERROR, INTERNAL_ERROR as the shared minimum vocabulary alongside the Gateway's existing UPSTREAM_UNAVAILABLE (Story 0). Every service reuses these; a service needing a new code adds it here, never invents a local one silently.
