<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>ru.mtsbank.cardoffice</groupId>
    <artifactId>backend</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>gateway</artifactId>
  <packaging>jar</packaging>
  <name>gateway</name>
  <description>
    Single frontend entry point (AD-6): the only container that publishes a host
    port. Routes one path per bounded-context service by docker-compose service
    DNS name. Story 0 scope: a minimal Spring Cloud Gateway app proving the
    route table end to end against each service's health endpoint -- no
    business routing/auth beyond the AD-7 trusted-header stub arrives later.
  </description>

  <properties>
    <!--
      Spring Cloud has not yet cut a release train against Spring Boot 4.1.x
      (the newest train, 2025.1.3, targets Boot 4.0.8), per Implementation
      Notes in the story file. Pinning the gateway starter's version directly,
      rather than importing the full spring-cloud-dependencies BOM, keeps
      every other artifact on this project's pinned Spring Boot 4.1.1 (parent
      dependencyManagement) instead of letting a whole second BOM re-manage
      spring-boot-starter- artifacts back down to 4.0.8.
    -->
    <spring-cloud-gateway.version>5.0.3</spring-cloud-gateway.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.cloud</groupId>
      <artifactId>spring-cloud-starter-gateway-server-webflux</artifactId>
      <version>${spring-cloud-gateway.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.projectreactor</groupId>
      <artifactId>reactor-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>gateway</finalName>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

</project>
