/**
 * JPA repositories implementing domain repository ports against this service's
 * own PostgreSQL schema (AD-1: DB-per-service, no shared schema). Empty in
 * Story 0 (infra scaffold only).
 */
package ru.mtsbank.cardoffice.riskblocks.adapters.out.persistence;
